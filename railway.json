{
  "$schema": "https://railway.app/json-schema/railway-schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfile": "habit-tracker-backend/Dockerfile",
    "context": "habit-tracker-backend"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "java ${JAVA_OPTS} -jar app.jar"
  }
}
